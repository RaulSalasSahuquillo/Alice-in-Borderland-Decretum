<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Alice in Borderland: Decretum"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AliceInBorderlandDECRETUM">

        <!-- Pantalla de inicio -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GamePlay1" />

        <!-- Pantalla de vídeo de introducción -->
        <activity
            android:name=".IntroActivity"
            android:exported="false"
            android:screenOrientation="sensorLandscape" />
        <!-- Quita screenOrientation si tu juego es vertical -->
    </application>

</manifest>
